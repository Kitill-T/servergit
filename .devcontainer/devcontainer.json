{
  "name": "Python FastAPI",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."  
  },
  "forwardPorts": [8000],
  "postCreateCommand": "pip install -e .",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  }
}